pipeline {
    agent any

    stages {
        stage('Check Node and NPM Versions') {
            steps {
                sh ' node --version '
                sh ' npm --version '
            }
        }
    }
}